{
  "name": "my-prayer-time",
  "version": "1.0.0",
  "description": "My Prayer Time — Islamic Prayer Times Alexa Skill for Echo Show",
  "private": true,
  "scripts": {
    "test": "cd lambda && npm test",
    "install:lambda": "cd lambda && npm install",
    "build": "cd lambda && npm install --production && cd .. && sam build",
    "local:invoke": "sam local invoke TawkitFunction --event events/launch.json"
  },
  "engines": { "node": ">=20" }
}
